import { AsyncStorageInterceptor } from './async-storage.interceptor';

describe('AsyncStorageInterceptor', () => {
  it('should be defined', () => {
    expect(new AsyncStorageInterceptor()).toBeDefined();
  });
});
